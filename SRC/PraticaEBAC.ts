function Multiplicar(numero1: number, numero2: number): number {
    return numero1 * numero2;
}
function Saudar(nome1: string): string {
    return `Olá ${nome1}`;
}

console.log(Multiplicar(10, 5));
console.log(Saudar("Diego"));