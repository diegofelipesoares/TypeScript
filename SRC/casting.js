"use strict";
var casting;
(function (casting) {
    let idade = 25;
    idade.toFixed();
    idade.length;
    idade.forEach(x => {
        console.log(x);
    });
    // let nome: string = 35 as unknown as String;
    // nome.
    // parseInt ("35");
    // parseFloat();
    // 10.toString;
})(casting || (casting = {}));
